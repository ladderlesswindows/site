/**
 * @file index.ts (root)
 * @description
 * Legacy entry point. Expo Router (see package.json "main": "expo-router/entry")
 * completely bypasses this file.
 * 
 * The previous reference to a non-existent ./App was dead code.
 * We keep a minimal safe stub so nothing breaks if this file is ever loaded.
 */

// Intentionally empty — expo-router/entry is the real entry for SDK 54+.
export {};
